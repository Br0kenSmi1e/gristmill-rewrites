[package]
name = "gristmill-rewrites-python"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
name = "_core"
crate-type = ["cdylib"]

[dependencies]
gristmill-rewrites = { path = ".." }
pyo3 = { version = "0.29.0", features = ["abi3-py311"] }
